query getLinks($where: LinkWhereInput!) {
  links(where: $where) {
    id
    title
    siteName
    url
    description
    image
  }
}

mutation createLink($data: CreateLinkInput!) {
  createLink(data: $data) {
    siteName
    title
    description
  }
}
