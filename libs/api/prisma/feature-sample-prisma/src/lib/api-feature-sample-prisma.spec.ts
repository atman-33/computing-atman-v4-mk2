import { apiFeatureSamplePrisma } from './api-feature-sample-prisma';

describe('apiFeatureSamplePrisma', () => {
  it('should work', () => {
    expect(apiFeatureSamplePrisma()).toEqual('api-feature-sample-prisma');
  });
});
